include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    # protobuf / grpc generated code — regenerated by `task proto:gen:dart`
    # and out of our control. Excluding keeps analyze focused on handwritten
    # source.
    - lib/infra/ipc/generated/**
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

linter:
  rules:
    # Common correctness rules that flutter_lints does not enable by default.
    avoid_empty_else: true
    avoid_relative_lib_imports: true
    avoid_slow_async_io: true
    cancel_subscriptions: true
    close_sinks: true
    control_flow_in_finally: true
    empty_statements: true
    hash_and_equals: true
    no_adjacent_strings_in_list: true
    throw_in_finally: true
    unnecessary_await_in_return: true
    unnecessary_statements: true
    unrelated_type_equality_checks: true
    valid_regexps: true
